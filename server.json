{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.prest/prest",
  "title": "pREST",
  "description": "Connect stdio-based MCP clients to a running pREST HTTP MCP endpoint for PostgreSQL-backed API exploration.",
  "repository": {
    "url": "https://github.com/prest/prest-mcp-adapter",
    "source": "github"
  },
  "version": "0.1.0",
  "packages": [
    {
      "registryType": "oci",
      "identifier": "ghcr.io/prest/prest-mcp-adapter",
      "version": "0.1.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "PREST_MCP_URL",
          "description": "URL of your running pREST HTTP MCP endpoint, for example http://localhost:3000/_mcp",
          "isRequired": true,
          "format": "string",
          "isSecret": false
        },
        {
          "name": "PREST_MCP_TOKEN",
          "description": "Optional bearer token used to authenticate with the pREST MCP endpoint.",
          "isRequired": false,
          "format": "string",
          "isSecret": true
        },
        {
          "name": "PREST_MCP_TIMEOUT_MS",
          "description": "Optional HTTP timeout in milliseconds. Defaults to 30000.",
          "isRequired": false,
          "format": "number",
          "isSecret": false
        }
      ]
    }
  ]
}
